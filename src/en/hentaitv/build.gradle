ext {
    extName = 'hentai.tv'
    extClass = '.HentaiTV'
    extVersionCode = 2
    isNsfw = true
}

apply plugin: "kei.plugins.extension.legacy"

dependencies {
    implementation(project(':lib:playlistutils'))
}

// v2.0 - rewrite: use guest API (guest.freeanimehentai.net) for browse/search
//        JSON-LD embedUrl extraction for video playback via nhplayer