ext {
    extName = 'hentai.tv'
    extClass = '.HentaiTV'
    extVersionCode = 3
    isNsfw = true
}

apply plugin: "kei.plugins.extension.legacy"

dependencies {
    implementation(project(':lib:playlistutils'))
}

// v3.0 - full rewrite: guest API primary, __NEXT_DATA__/nhplayer fallback
//        TriState tag filters, censored filter, tag-mode AND/OR
//        Fix API params (ordering/ordering_direction)
//        Fix episode URLs (/hentai/{series}/ep-{N}/)

