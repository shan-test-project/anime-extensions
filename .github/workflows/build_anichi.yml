name: Build Anichi APK

on:
  push:
    branches: [master]
    paths:
      - 'src/en/anichi/**'
      - 'lib-multisrc/anikototheme/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build Anichi
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      - name: Generate debug signing key
        run: |
          keytool -genkeypair -v \
            -keystore signingkey.jks \
            -alias key0 \
            -keyalg RSA -keysize 2048 \
            -validity 10000 \
            -storepass android \
            -keypass android \
            -dname "CN=Anichi, OU=Extensions, O=Anichi, L=Unknown, ST=Unknown, C=US"
          echo "KEY_STORE_PASSWORD=android" >> $GITHUB_ENV
          echo "ALIAS=key0" >> $GITHUB_ENV
          echo "KEY_PASSWORD=android" >> $GITHUB_ENV

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Anichi APK
        run: ./gradlew :src:en:anichi:assembleRelease -x spotlessCheck

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Anichi-APK
          path: src/en/anichi/build/outputs/apk/release/*.apk
          if-no-files-found: error
